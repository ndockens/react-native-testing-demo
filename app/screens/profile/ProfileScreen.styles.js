import { StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    padding: 20,
  },
  logOutButton: {
    color: 'darkred',
  },
  screenTitle: {
    fontSize: 24,
    textAlign: 'center',
  },
  welcomeMessage: {
    fontSize: 30,
    marginTop: 40,
    textAlign: 'center',
  },
});